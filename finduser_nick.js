(await (await fetch('/graphql', { method: 'POST', headers: { 'X-Token': JSON.parse(document.getElementById("__NEXT_DATA__").innerText).props.initialState.common.user.xToken, 'x-client-type': 'Client', 'CSRF-Token': document.querySelector("[name='csrf-token']").content, 'Content-Type': 'application/json' }, body: JSON.stringify({ query: `query ($nickname: String) { user(nickname: $nickname) { id } }`, variables:{ nickname: '<nickname>' } }) })).json()).data.user.id
